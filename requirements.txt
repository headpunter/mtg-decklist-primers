# Core dependencies
requests>=2.31.0          # HTTP for Archidekt + Scryfall APIs
pyyaml>=6.0               # read/write meta.yaml and owned.yaml

# Primer builder
google-generativeai>=0.7  # Gemini API (build_primer.py)
